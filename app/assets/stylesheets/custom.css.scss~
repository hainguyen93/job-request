@import "bootstrap-sprockets";
@import "bootstrap";

$gray-medium-light: #eaeaea;

/* universal */

html {
  overflow-y: scroll;   /* always show scroll bar */
  position: relative;
  min-height: 100%;      
}

/* main body */

body{
  margin: 0 0 100px; /* bottom = footer height */
  padding-top: 60px;
  color: #263248;
}

/* forms validation */

#error_explanation {
  color: red;
  ul {
    color: red;
    margin: 0 0 30px 0;
  }
}

.field_with_errors {
  @extend .has-error;
  .form-control {
    color: $state-danger-text;
  }
}

section{
  overflow: auto;
}

textarea {
  resize: vertical;
}

.center{
  text-align: center;
  h1 {
    margin-bottom: 10px;
  }
}

/* typography */

h1, h2, h3, h4, h5, h6 {
  line-height: 1;
}

h1 {
  font-size: 3em;
  letter-spacing: -2px;
  margin-bottom: 30px;
  text-align: center;
  color: #ff9800;
}

/* all buttons */

.btn-primary {
  background-color: #7e8aa2;
  &:hover {
    background-color: #6A7CA1;
  }
}

/* accepted job symbol */
.glyphicon-ok, #home {
  font-size: 150%;  
}

p {
  font-size: 1.1em;
  line-height: 1.7em;
}

/* header */

#header {
  background-color: #263248; 
}

@font-face {
  font-family: 'header_slim';
  src: url('../fonts/raleway_thin-webfont.eot'); /* IE9 Compat Modes */
  src: url('../fonts/raleway_thin-webfont.eot?#iefix') format('embedded-opentype'), /* IE6-IE8 */
       url('../fonts/raleway_thin-webfont.woff') format('woff'), /* Pretty Modern Browsers */
       url('../fonts/raleway_thin-webfont.ttf')  format('truetype'), /* Safari, Android, iOS */
       url('../fonts/raleway_thin-webfont.svg#svgFontName') format('svg'); /* Legacy iOS */
}

/* main content */

.wrapper {
  margin: 0px auto;  
  width: 80%;  
}

/* main content */

#main-content {
  padding-bottom: 100px;  

  #inner-content {
    padding-top: 20px;
  }
}

/* logo 'Job Request System' */

#logo {
  font-family: 'header_slim';
  float: left;  
  font-size: 1.7em;
  color: white;
  text-transform: uppercase;
  padding-top: 9px;
  font-weight: bold;
  &:hover {   
    text-decoration: none;
  }
}

/* home button */

#home-btn{  
  font-weight: bold;
}

/* footer */

footer { 
  position: absolute;
  margin-bottom: 0px;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 50px;
  background-color: #263248; 

  a {   /* links in footer */
    color: #A1A1A1;
    &:hover {
      color: $gray-lighter;
      text-decoration: none;
    }
  }
  
  small {  /* copyright area */
    margin-top: 15px;
    float: left;
    color: #ffffff;
    font-family: 'header_slim';
    font-size: 110%;
    font-weight: bold;
  }
  
  ul {  /* links area */
    margin-top: 15px;
    float: right;
    font-size: 105%;
    list-style: none;
    li {
      float: left;
      margin-left: 15px;
      margin-right: 10px;
    }
  }
}


/* rigs index page */

.index {
 
  list-style: none;
  padding: 3px 0px 5px 0px; 
  background-color: #EBEBEB;
  margin: 0px 15px 30px 15px; 
  
  .rig-block {  /* rig element */
    padding: 5px 0px;
    border-top-left-radius: 40px;
    border-bottom-left-radius: 40px;  
    margin: 2px 5px 10px 5px;     
    background-color: $gray-lighter;
      
    &:hover {
      color: #263248;
    } 
    
    .rig-title {   /* rig code */
      padding: 5px 0px;
    }
  } 

  .user-block { 
    border-top-left-radius: 40px;
    border-bottom-left-radius: 40px;
  }

  .job-block{
    border-top-left-radius: 45px;
    border-bottom-left-radius: 45px;    
  }


  li { 
    background-color: white;    
    overflow: auto;
    margin: 3px 5px 5px 5px;
    padding: 2px 0px;
    border: 2px solid #D9D9D9; 

    .deadline{ 
      padding: 5px 0px;
    }

    .timestamp {
      padding-left: 0px;

    }

    .title {
      padding-left: 0px;
      font-size: 1.2em;
      a {
        color: #263248; 
      }      
    }  

    .btn, .disappear {
      display: none;
    }    
   
    &:hover {       
       background: #FFE0B5; 
      .btn, .disappear {       
        display: block;
      }
      
    }

    /* avatar in users index */
  
    .user-avatar{     
      border-radius: 50%; 
      margin: 4px auto 0px;     
      max-width:100%;
      max-height:100%; 
    }

    /* job image in active jobs page,... */

    .job-avatar{   
      border-radius: 50%;
      margin-top: 10px;
      max-width:100%;
      max-height:100%; 
    }

    /* avatar in completed jobs page */
    
    .job-done-avatar{ 
      margin: 16px 13px;
      font-size: 250%;
    }

    .rig-avatar {
      padding-top: 4px;
      border-radius: 30px;
      font-size: 200%;
      max-width:100%;
      max-height:100%;      
    }
  }
   
  /* job block in completed job page */

  .job-done-block { 
    margin-left: 0px;
    margin-bottom: 8px;
    background-color: $gray-lighter;
  }
}

.rig-index, .job-done-index {
  background-color: white;
}

/* heading likes 'urgent jobs', 'current jobs' */

.sections { 
  background-color: #7e8aa2;  
  padding: 5px 0px 5px 10px;
  color: white;
  font-size: 140%;
  margin: 0px 15px;
  /* border-top-left-radius: 20px; */ 
}

/* job description */

.job-des-content {
  padding-left: 15px; 

  .active {   /* job status */
    color: green;
  }

  .completed {   /* job status */
    color: yellow;
  }

  .overdue {   /* job status */
    color: red;
  }
}

/* comment post */

.cmt-list {
  list-style: none;
  padding: 0px;  

  /* comment elem */

  .cmt-block {
    margin: 5px 0px; 
    padding: 10px 0px 5px; 
    border-top: 2px solid #DBDBDB;      

    .timestamp {
      color: #B3B3B3;

      &:hover {
        color: #263248;
      }
    }
  
    .title {     
      font-size: 110%;
      margin-bottom: 8px;
    }

    .remove-btn {    
      display: none;      
    }

    &:hover{
      .remove-btn {
        display: block;
      } 
    }
   
    .cmt-content {
      margin:  0px;
      padding: 0px; 
    }

    .cmt-upload{     
      margin-top: 10px;
      padding: 0px;
    }
  }  
}

/* tab in job description page */
.job-tab{
  margin-top: 20px;
}

.cmt-text-box{
  margin: 0px;
  background-color: $gray-lighter;
  padding: 10px 0px;  
}

.time-record-box{
  margin: 0px;  
  background-color: $gray-lighter;
  padding: 20px 0px; 
  
}

/* avatar in comment section */

.img-rounded { 
  max-width: 100%;
  max-height: 100%; 
}   


/* job description */

.outer-box {  
  margin: 0px;
  .box {
    padding: 5px; 
    margin: 2px 0px;  
   
  }

  .who, .des, .document {
    padding: 0px; 
    li {
      margin-left: 20px;
    } 
  }
  
  .des {
    padding: 20px;
    background-color: #EBEBEB;
  } 

  .acc-list {
    margin: 0px;  
    background-color: $gray-lighter;
    padding: 15px 0px;  

    .acc-name{
      padding-left: 20px;
    }
  }

  .heading{
    color: white;
    font-weight: bold;
    background-color: #7e8aa2;
    padding: 5px 0px 5px 20px;  
      
  }

  .title {   
    font-weight: bold; 
    background-color: #EBEBEB;
  }

  .active{
    color: green;
    font-weight: bold;
    background-color: #EBEBEB;
  }

  .overdue{
    background-color: #EBEBEB;
    color: red;
    font-weight: bold;
  }

  .content {
    background-color: #EBEBEB;
    padding-left: 20px;
  }  
}

/* comment text area buttons */

.upload-btn {
   margin-top: 10px; 
   padding-left: 0px;
}
 
.post-btn {
  margin: 0px ;
  margin-top: 10px;
}

.author {
  color: #7e8aa2;
}

.forgot_pass {
  padding: 0px; 
}

/* text con contact page */
#contact-msg{
  font-size: 110%;
  background-color: $gray-lighter;
  padding: 10px;
}

.accepted-sign{
  background-color: $gray-light;
}













